<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="description" content="">
	<meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Tipsyology</title>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <link rel='stylesheet' href='//fonts.googleapis.com/css?family=Open+Sans:400,300,600,700,800'>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="/whhg-font/css/whhg.css">
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body ng-app='tipsy' ng-controller="FindCtrl">

<div class="wrapper">
  <%= render 'toolbar' %>
  <div id="main-cont" class="grid">
    <div class="content grid-cell">
      <ng-include src="'/find-results.html'"></ng-include>
      <%= yield %>
    </div>
    <aside class="fancy-sidebar grid-cell">
      <div class="sidebar-controls"></div>
      <div class="ingredients-list">
        <ul>
          <li class="sidebar-list-first">My Cabinet <i class="icon-fridge"></i></li>
          <li ng-repeat="item in cabinet" class="item">
            {{item.name}} 
            <a ng-click="removeFromCabinet(item)"><i class="fa fa-times-circle"></i></a>
          </li>
          <li ng-show="!cabinet || !cabinet.length" style="font-style:italic">(empty)</li>
          <li><a><i class="icon-circlefork"></i> What can I make?</a></li>
        </ul>
      </div>
      <div class="shopping-list">
        <ul>
          <li class="sidebar-list-first">Shopping List <i class="fa fa-shopping-cart"></i></li>
          <li ng-repeat="item in shoppingList" class="item">
            {{item.name}}
            <a ng-click="removeFromShoppingList(item)"><i class="fa fa-times-circle"></i></a>
          </li>
          <li ng-show="!shoppingList || !shoppingList.length" style="font-style:italic">(empty)</li>
        </ul>
      </div>
    </aside>
</div>

</body>
</html>
